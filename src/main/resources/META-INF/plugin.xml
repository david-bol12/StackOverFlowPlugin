<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>com.example.stackoverflow-plugin</id>
    <name>Stack Overflow Error Search</name>
    <vendor>YourCompany</vendor>
    <description><![CDATA[
        Search Stack Overflow for errors without leaving the IDE.<br/>
        <ul>
            <li>Build errors are <b>automatically</b> populated in the Stack Overflow Search tool window</li>
            <li>Right-click selected error text in the editor → <b>Search Stack Overflow</b></li>
            <li>Place the caret on an underlined error and press <b>Ctrl+Shift+F1</b></li>
        </ul>
    ]]></description>

    <!-- Base requirement: Compose UI runtime -->
    <depends>com.intellij.modules.compose</depends>

    <!--
        com.intellij.java is optional so the plugin loads in CLion (no Java plugin).
        plugin-idea.xml registers the CompilationStatusListener only when present.
    -->
    <depends optional="true" config-file="plugin-idea.xml">com.intellij.java</depends>

    <resource-bundle>messages.MyMessageBundle</resource-bundle>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow id="Stack Overflow Search"
                    factoryClass="com.example.MyToolWindowFactory"
                    anchor="right"
                    icon="AllIcons.Toolwindows.ToolWindowPalette"/>
        <projectService serviceImplementation="com.example.StackOverflowSearchService"/>
        <postStartupActivity implementation="com.example.ClionBuildSubscriber"/>
        <postStartupActivity implementation="com.example.PyCharmBuildSubscriber"/>
    </extensions>

    <actions>
        <action id="com.example.SearchStackOverflow"
                class="com.example.SearchStackOverflowAction"
                text="Search Stack Overflow"
                description="Search Stack Overflow for the selected text or the error at the caret">
            <keyboard-shortcut keymap="$default" first-keystroke="ctrl shift F1"/>
            <add-to-group group-id="EditorPopupMenu" anchor="last"/>
        </action>
    </actions>
</idea-plugin>
